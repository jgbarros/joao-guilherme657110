FROM eclipse-temurin:21-jre-alpine
WORKDIR /app
# Placeholder até mvn package
RUN echo "Backend placeholder - compile com mvn package primeiro" > README.md
EXPOSE 8080
CMD ["tail", "-f", "/dev/null"]  # Mantém container vivo
